import { h } from 'preact';
import { TypedComponent } from '@/common/typings/prop-types';

export const PasswordList: TypedComponent<Props> = () => (
    <section>Password list page</section>
);

export interface Props {}
