import { createSelector } from 'reselect';
import { AppState } from '@/store';
import { {{pascalCase name}}State } from './{{camelCase name}}.model';

const select{{pascalCase name}} = (
    state: AppState
): {{pascalCase name}}State => state.{{camelCase name}};
