import { AppState } from '@/store';
import { mergeState } from '@/common/utils/store';
import {
    {{camelCase name}}Emitter,
    {{camelCase name}}EventTypes,
} from '@/modules/{{camelCase name}}/{{camelCase name}}.events';

const change = mergeState('{{camelCase name}}');

export const {{camelCase name}}State = {} as const;

export type {{pascalCase name}}State = typeof {{camelCase name}}State;

export const {{camelCase name}}Actions = {
    noop(
        appState: AppState,
    ): Partial<AppState> {
        return change({});
    },
} as const;

export { useAction } from '@preact-hooks/unistore';
